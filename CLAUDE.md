# CLAUDE.md

## 目的
このリポジトリは、Gradio UI と Oracle Database 連携を伴う Python 製 AI Agent アプリケーションを開発する。
最優先事項は、正確性・保守性・運用上の安全性、そして「特定の例やテストフィクスチャ向けの最適化」ではなく**実世界の多様なケースへの汎化**である。

## 基本原則
- コーディング前に考える。関連ファイルを読み、現状のアーキテクチャと制約を把握し、非自明な変更には短い計画を立ててから着手する。
- 要件を満たす**最小・最単純**の設計を選ぶ。明確な正当化なしに新しいフレームワーク・抽象・依存を持ち込まない。
- 外科手術的な変更を行う。タスクが明示的にリファクタリングを要求しない限り、既存のアーキテクチャとコーディングパターンを保つ。
- 実装前に成功基準を定義し、テスト・静的解析・実行時検証で確認する。
- 症状ではなく根本原因を直す。業務ルールとして明示されない限り、ハードコードされた特殊分岐を避ける。
- すべての機能追加・バグ修正を、**リグレッション・隠れた結合・テストへの過学習**のリスクとして扱う。

## 汎化ガードレール（最重要）
「現在のテストを通すこと」を目的に最適化しない。それが以下への過学習を招くなら避ける:
特定のフィクスチャ・プロンプト・データセット・モック・再現スクリプト。

修正・改善を行う際、その変更が次に該当しないか必ず点検する:
- 報告された例だけを処理し、近傍のバリアントで壊れる
- 脆い文字列マッチ・マジック定数・一回限りの分岐・プロンプト固有のヒューリスティクスに依存する
- 既存の抽象や検証層を壊す／迂回する
- 単一テストを満たすために挙動を黙って狭める
- 本番と乖離したモック挙動に強く依存する

確定前に自問する:
- このケースの背後にある**より一般的なルール**は何か。
- 隣接する入力やワークフローも動くべきではないか。
- 現在のテストでカバーされていない本番シナリオは何か。
- この変更は症状をエンコードしているのか、不変条件をエンコードしているのか。

テストが汎用性を損なう挙動を要求しているように見える場合は、それを明示的に指摘し、より安全な実装を提案する。

## ワークフロー
非自明な作業では次の順序に従う:
1. 関連モジュール・インターフェース・テスト・設定を調査する。
2. 現状の挙動と推定される根本原因を要約する。
3. 簡潔な実装計画を書く。
4. 最小で正しい変更を実装する。
5. 対象範囲の限定的検証 → 必要に応じて広範なリグレッション検証。
6. 変更が汎用的・本番安全で、トリガとなったテストに過学習していないか確認する。
7. 何を・なぜ正しく・何を検証したかを要約する。

ごく小さく自明な編集では計画の文書化は不要だが、変更前に周辺コードは必ず確認する。

## Python ルール
- リポジトリが別途指定しない限り Python 3.11+ 互換で書く。
- 巧妙さより明示性・可読性を優先する。
- 公開関数と重要な内部インターフェースには型ヒントを付ける。
- 関数は小さく凝集させる。推論性・テスト容易性が上がるなら巨大関数を分割する。
- Agent の入出力には適切に dataclass・TypedDict・構造化モデルを用いる。
- 隠れたグローバル状態を避け、依存は可能な限り明示的に渡す。
- 構造化されたエラーハンドリングを行い、例外を黙って握りつぶさない。
- フォーマット・lint・import 順序・モジュール構成は既存の規約に従う。

## AI Agent ルール
- オーケストレーション／ツールロジック／プロンプト・コンテキスト構築／ドメインロジック／UI を明確に分離する。
- 業務ルールはプロンプトに埋め込まず、決定論的なコードに置く。
- プロンプトは簡潔・明示的・保守可能に保つ。肥大化と指示の重複を避ける。
- 境界で Agent の入出力を検証する。LLM 出力はパース・検証・正規化されるまで**信頼しない**。
- モデルが常に「正しく振る舞う」ことに頼らず、決定論的ガード・スキーマ（JSON Schema 等の構造化出力）・後処理を優先する。
- 挙動変更時はマルチターンの影響・ツール呼び出し順序・リトライ・フォールバック・可観測性・トークンコストを考慮する。
- 1 つのデモ会話を改善する代わりに一般的な堅牢性を下げる変更を避ける。
- Agent の失敗を直す際は、原因がプロンプト設計・コンテキスト選択・ツール契約の不一致・パーサの脆さ・状態管理・評価の弱さのどれかを特定する。

## Oracle Database ルール
- 安全で明示的な SQL と予測可能なトランザクション制御を優先する。
- 認証情報・DSN・ウォレット内容・シークレットをコード・テスト・ログ・例に**埋め込まない**。
- 必ずバインド変数／パラメータ化実行を使う。ユーザー入力を文字列連結で SQL に組み立てない。
- トランザクション境界・セッション状態・コネクションプール・リソース解放（カーソル/接続のクローズ）に注意する。
- 必要に応じて Oracle 固有の挙動（SQL セマンティクス・データ型・並行性・実行計画の感度）を考慮する。
- 性能関連の変更は推測でなく計測に基づき、変更は最小限にする。
- タスクが明示的に要求しない限り、アプリケーションコードでスキーマ変更を行わない。

## Gradio ルール
- UI コードは薄く保ち、対話フローに集中させる。
- ドメインロジックは Gradio 層の外に置き、UI を起動せずにテスト可能にする。
- ユーザー入力は Agent／DB 層に渡す前に検証・サニタイズする。
- 長時間処理はキュー／非同期で扱い UI スレッドをブロックしない。並行性は明示的に制御する。
- コンポーネントの状態遷移は予測可能にし、暗黙の副作用よりも明示的なイベント結線を選ぶ。
- 1 つのデモ入力やスクリーンショットに合わせて UI 挙動をハードコードしない。基盤ワークフローが汎用なら汎用に保つ。
- UI 上でも明確なエラー報告と応答性を維持する。

## テスト方針
- 意味のあるバグ修正には可能な限りリグレッションテストを付ける。
- 報告ケースの再現で止めない。**一般ルールを検証する近傍ケースと対照ケース**を追加する。
- 実装の些末な内部ではなく、挙動と不変条件を検証する。
- 偶発的な現状挙動を固定するだけのテストは、それが明示的に要求される場合を除き避ける。
- モックの使用は慎重に。過剰なモックは統合不具合を隠し過学習を助長する。
- DB 関連は可能なら現実的な統合テストを優先する。
- Agent 関連は言い換え・隣接意図・不正入力・境界条件をカバーする評価ケースを含める。
- 適切な単体／統合／E2E の組み合わせでテストする。

## 完了前の検証
タスク完了を宣言する前に:
- 変更範囲の最小テストを実行する。
- 範囲に応じた広範なリグレッションを実行する。
- 設定されていれば lint／型／フォーマットチェックを実行する。
- import・シグネチャ・設定の整合を確認する。
- diff に不要な変更がないか確認する。
- トリガとなったテストや例に過学習していないか確認する。
- シークレットや環境固有値が混入していないか確認する。

実際の検証の根拠なしに成功を主張しない。

## 変更スタイル
- タスクが明示的に許可しない限り後方互換性を保つ。
- diff はレビューしやすく保つ。
- タスクと無関係な便乗リファクタリングを避ける。
- スコープ外の大きな問題に気づいたら、黙って変更を広げず別途指摘する。
- 複数の方法がある場合、推論・検証・保守が最も容易なものを選ぶ。

## 報告（コミュニケーション）
報告時には次を述べる:
- 根本原因
- 採用した修正
- それがトリガケースを超えて汎化する理由
- 実行したテスト／チェック
- 残存リスク・前提・フォローアップ項目

## プロジェクト参照
- 概要は README.md。
- Python ツール・依存規約は pyproject.toml／requirements を参照。
- 既存のテストパターンはテストディレクトリを参照。
- Agent・Oracle・デプロイ規約は docs/ や設計文書があれば参照。

## コンテキスト圧縮時の保持事項
コンテキストを圧縮する際は次を保持する:
- タスクの目的
- 変更したファイル
- 実装計画
- 検証コマンドと結果
- 未解決のリスク・前提
- テスト過学習の回避と汎化維持に関する議論
